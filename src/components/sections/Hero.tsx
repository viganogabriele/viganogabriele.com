import { motion, useReducedMotion, useScroll, useTransform } from "framer-motion";
import { ArrowDownRight, ArrowUpRight, Power } from "lucide-react";
import { Magnetic } from "../motion/Magnetic";
import { TextScramble } from "../motion/TextScramble";
import { FaceHero3D } from "../ui/FaceHero3D";
import { ease } from "../../lib/motion";

export function Hero({ onNavigate, onToggleSystem, systemActive }: { onNavigate: (target: string) => void; onToggleSystem: () => void; systemActive: boolean }) {
  const reduced = useReducedMotion();
  const { scrollY } = useScroll();
  const wordmarkScale = useTransform(scrollY, [0, 420], [1, 0.86]);
  const wordmarkY = useTransform(scrollY, [0, 420], [0, -44]);
  const portraitOpacity = useTransform(scrollY, [0, 420], [1, 0.28]);
  const go = (event: React.MouseEvent<HTMLAnchorElement>) => { event.preventDefault(); onNavigate("#projects"); };
  return <section id="top" className="hero-grid relative flex min-h-[100svh] items-center overflow-hidden border-b border-white/[0.07] pt-28"><div className="hero-scanlines absolute inset-0 opacity-80" /><div className="pointer-events-none absolute inset-x-0 top-[18%] h-px bg-gradient-to-r from-transparent via-cyan-100/20 to-transparent" /><div className="relative mx-auto grid w-full max-w-7xl gap-10 px-5 pb-16 sm:px-8 lg:grid-cols-[1.35fr_0.65fr] lg:items-center lg:px-10"><motion.div style={reduced ? undefined : { scale: wordmarkScale, y: wordmarkY }} className="relative z-10"><motion.p initial={reduced ? false : { opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: 0.16, duration: 0.6, ease: ease.cinematic }} className="font-mono text-[10px] uppercase tracking-[0.2em] text-cyan-100/75">Computer Engineering Student · Milan</motion.p><motion.h1 initial={reduced ? false : { opacity: 0, y: 28 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: 0.23, duration: 0.9, ease: ease.cinematic }} className="mt-5 max-w-4xl text-[17vw] font-medium leading-[0.76] tracking-[-0.09em] text-[#f2f3f5] sm:text-[13vw] lg:text-[7.65rem] xl:text-[9.25rem]"><span className="block">GABRIELE</span><span className="block pl-[0.06em] text-zinc-300">VIGANÒ</span></motion.h1><motion.div initial={reduced ? false : { opacity: 0, y: 14 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: 0.45, duration: 0.6, ease: ease.softSettle }} className="mt-10"><p className="text-3xl font-medium tracking-[-0.05em] text-cyan-100 sm:text-4xl lg:text-5xl"><TextScramble text="I build cool things." /></p><div className="mt-7 grid max-w-xl grid-cols-3 border-y border-white/[0.09] py-3 font-mono text-[9px] uppercase tracking-[0.13em] text-zinc-500"><span>Role<br /><b className="mt-1 block font-normal text-zinc-300">Product / UX</b></span><span>Status<br /><b className="mt-1 block font-normal text-amber-200">Building</b></span><span>Stack<br /><b className="mt-1 block font-normal text-zinc-300">Web / Systems</b></span></div><div className="mt-8 flex flex-wrap items-center gap-3"><Magnetic><a href="#projects" onClick={go} data-cursor="hover" className="inline-flex items-center gap-3 bg-[#f2f3f5] px-5 py-3.5 text-sm font-semibold text-black transition-colors hover:bg-cyan-100">See the work <ArrowDownRight className="h-4 w-4" /></a></Magnetic><button type="button" onClick={onToggleSystem} data-cursor="hover" className="inline-flex items-center gap-2 border border-white/[0.12] px-4 py-3.5 font-mono text-[10px] uppercase tracking-[0.13em] text-zinc-300 transition-colors hover:border-cyan-200/50 hover:text-cyan-100"><Power className="h-3.5 w-3.5" />System mode <span className="hidden text-zinc-600 sm:inline">Shift + S</span></button></div></motion.div></motion.div><motion.div initial={reduced ? false : { opacity: 0, x: 30 }} animate={{ opacity: 1, x: 0 }} transition={{ delay: 0.34, duration: 1, ease: ease.cinematic }} style={reduced ? undefined : { opacity: portraitOpacity }} className="relative pt-4 lg:pt-0"><FaceHero3D systemActive={systemActive} /><span className="section-anchor-label absolute bottom-[-3rem] right-0 font-mono text-[9px] uppercase tracking-[0.17em] text-cyan-100/75">HERO / X-00 Y-00</span></motion.div></div><a href="#about" onClick={(event) => { event.preventDefault(); onNavigate("#about"); }} data-cursor="hover" className="absolute bottom-6 left-5 flex items-center gap-3 font-mono text-[9px] uppercase tracking-[0.16em] text-zinc-500 transition-colors hover:text-white sm:left-8 lg:left-10">Scroll to inspect <ArrowUpRight className="h-3 w-3 rotate-90" /></a></section>;
}
